# Fitbit OAuth Configuration
# Copy this file to .env and fill in your values

# Your Fitbit App Client ID (from https://dev.fitbit.com/apps)
CLIENT_ID=your_client_id_here

# Your Fitbit App Client Secret (from https://dev.fitbit.com/apps)
CLIENT_SECRET=your_client_secret_here

# Redirect URL - Must match the callback URL in your Fitbit App settings
# For local/homelab deployment, use your local IP address
REDIRECT_URL=http://192.168.13.5:5032/

# For production deployment on a domain:
# REDIRECT_URL=https://yourdomain.com/

# Security Configuration (Optional but Recommended)

# Dashboard Password - Protects the web UI
# Leave empty to disable password protection
DASHBOARD_PASSWORD=your_secure_password_here

# API Key - Protects REST API endpoints for MCP/external access
# Leave empty to disable API key protection (not recommended for production)
# Generate a strong key: openssl rand -hex 32
API_KEY=your_secure_api_key_here

# Logging Configuration (Optional)

# LOG_LEVEL - Controls verbosity of application logs
# Options: CRITICAL, ERROR, WARN, INFO, DEBUG, TRACE
# Default: INFO (recommended for production)
LOG_LEVEL=INFO

# Log Level Guide:
# - CRITICAL/FATAL: Only fatal errors that will crash the app (least verbose)
# - ERROR: Serious errors that don't crash the app  
# - WARN/WARNING: Potential issues, warnings about deprecated features
# - INFO: Normal operational messages - default level (recommended)
# - DEBUG: Detailed diagnostic information for troubleshooting
# - TRACE: Most verbose - step-by-step execution including all [CACHE_DEBUG] messages

